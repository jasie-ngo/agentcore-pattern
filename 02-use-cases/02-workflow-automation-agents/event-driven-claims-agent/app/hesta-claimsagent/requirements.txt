# Bounds mirror pyproject.toml, which is the source of truth for this app.
# The Dockerfile does NOT install from this file — it runs `uv sync --frozen`
# against pyproject.toml + uv.lock, so the lockfile is what pins the deployed
# container. This file exists only as a convenience for plain-pip local setups
# (see docs/deployment.md) and must be kept in sync with pyproject.toml by hand.
aws-opentelemetry-distro>=0.18.0,<1.0
bedrock-agentcore>=1.15.0,<2.0
botocore[crt]>=1.43.36,<2.0
mcp>=1.28.0,<2.0
pyyaml>=6.0,<7.0
strands-agents>=1.44.0,<2.0
